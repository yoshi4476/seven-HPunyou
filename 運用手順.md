# 運用手順

> このサイトの日常運用と、詰まったときの対処をまとめたものです。

## 0. 役割分担 — 記事作成は「管制塔」が行う

2026-07-30 に、記事の作成をグループ共通の自動化エンジン
[SS-AIO-LP](https://github.com/yoshi4476/SS-AIO-LP)(自動化管制塔)へ移管しました。

| 担当 | 役割 |
|---|---|
| **管制塔** (SS-AIO-LP) | キーワード選定・執筆・品質審査・画像生成・3サイト横断のカニバリ防止・月次レポート |
| **このリポジトリ** | 納品された記事を受け取り、HTML化して公開する(受け入れ役) |

管制塔は3サイト(AI集客ラボ / コーポレート / この補助金サイト)を一元管理し、
キーワード台帳を共有することで**サイト間で記事テーマが食い合わないよう**制御しています。

このリポジトリの記事生成(`auto-publish.yml`)と月次レポートは**停止済み**です。
二重生成を防ぐため、再開する場合は必ず管制塔側の補助金枠を先に止めてください。

## 1. 日常運用 — 基本は「何もしなくていい」

記事の作成・公開は全自動です。パソコンを閉じていても、クラウド上で動きます。

| 時刻 | 自動で起きること | 実行元 |
|---|---|---|
| 毎日 16:00 | 補助金サイト向けの記事を1本作成し、このリポジトリへ納品 | 管制塔 |
| 納品直後 | 記事をHTML化 → 一覧・sitemap・llms.txt を再生成 → 本番へ公開 | このリポジトリ |
| 毎週月 10:23 | キーワード補充・重複検査・既存記事のリライト | 管制塔 |
| 毎月1日 9:00 | 3サイト横断の月次レポートを作成し info.ai@7senses.co.jp へ送付 | 管制塔 |

**確認したいとき**
- 記事が作られたか: https://github.com/yoshi4476/SS-AIO-LP/actions
- 公開されたか: https://github.com/yoshi4476/seven-HPunyou/actions

## 2. サイトの文言を直したいとき

```powershell
git push
```

これだけです。ファイルを修正して `git push` すると、1分ほどで本番サイト(https://lp.7senses.co.jp)へ自動反映されます。

反映状況は Actions タブの「**自動デプロイ**」で確認できます。ログの末尾にHTTPステータスが出るので、`200` なら成功です。

> 記事を生成せずに再公開だけしたい場合は、Actions → 「自動デプロイ」→ Run workflow でも実行できます。

## 3. Google Search Console の登録

`lp.7senses.co.jp` は CNAME で Cloudflare を指しているため、**「ドメイン」プロパティ(DNS認証)は使えません**。必ず「URLプレフィックス」を使ってください。

1. https://search.google.com/search-console/welcome を開く
2. **右側の「URLプレフィックス」**に `https://lp.7senses.co.jp/` を入力 → 続行
3. 確認方法「**HTMLファイル**」を選ぶ(所有権確認ファイルはサイトに設置済み)
4. 「**確認**」を押す

確認が通ったら、左メニュー「サイトマップ」から `sitemap.xml` を送信します。

## 4. 困ったときの対処

### push できない

```
fatal: The current branch master has no upstream branch
```

→ 上流の設定が外れています。

```powershell
git branch --set-upstream-to=origin/master master
git push
```

### push が拒否される (rejected)

自動実行と同時に更新した場合に起きます。

```powershell
git pull --rebase
git push
```

### 自動デプロイが失敗する

Cloudflare のAPIトークンが失効している可能性があります。Cloudflareダッシュボードで
**Pages編集権限のトークンを再発行** し、GitHubの
Settings → Secrets and variables → Actions → `CLOUDFLARE_API_TOKEN` を更新してください。

### 記事が公開されない日がある

安全チェック(景表法・出典・リンク切れなど)で止まった記事は `blog-system/content/human-review/`
に隔離されます。同日中に自動で再挑戦しますが、2回失敗すると人の確認待ちになります。

## 5. まだ接続していないもの

| 項目 | 状態 | 影響 |
|---|---|---|
| フォームの受信確認 | GASは接続済みだが着信未確認 | 通知が届かない場合、問い合わせが消える |
| サイト無料診断API | 未実装のGASが稼働中 | URL診断が「調整中」表示になる |
| GA4 | 計測タグ設置済み | 数日でレポートにデータが入る |
| Search Console | 登録作業中 | 検索キーワードの分析ができない |

### フォームの受信確認(最優先)

`info.ai@7senses.co.jp` に「接続テスト」という件名のメールが届いているか確認してください。
届いていなければ、フォームは「送信完了」と表示しつつ問い合わせが失われる状態です。

## 6. 月次レポートに実データを接続する

毎月1日に生成されるコンサルレポートへ、GA4とSearch Consoleの実数値を取り込む設定です。
一度やれば以降は自動です。

### STEP 1 — Google Cloud でサービスアカウントを作る

1. https://console.cloud.google.com/ を開き、プロジェクトを新規作成(名前は任意)
2. 「APIとサービス」→「ライブラリ」で次の**2つを有効化**
   - `Google Analytics Data API`
   - `Google Search Console API`
3. 「APIとサービス」→「認証情報」→「認証情報を作成」→「**サービスアカウント**」
   名前は任意(例: `report-reader`)。ロールの指定は不要
4. 作成したサービスアカウントを開き「キー」タブ →「鍵を追加」→「新しい鍵を作成」→ **JSON** を選択
   → JSONファイルがダウンロードされる
5. サービスアカウントの**メールアドレス**(`...@....iam.gserviceaccount.com`)を控える

### STEP 2 — GA4 に読み取り権限を与える

1. GA4 の管理(歯車)→「プロパティのアクセス管理」
2. 右上の「＋」→「ユーザーを追加」→ STEP1のメールアドレスを入力
3. 役割は「**閲覧者**」→ 追加
4. 管理 →「プロパティの詳細」で **プロパティID**(9〜10桁の数字)を控える
   ※ `G-` で始まる測定IDや、ストリームIDとは別物です

### STEP 3 — Search Console に読み取り権限を与える

1. 設定 →「ユーザーと権限」→「ユーザーを追加」
2. STEP1のメールアドレスを入力、権限は「**制限付き**」でよい

### STEP 4 — GitHub にシークレットを登録

https://github.com/yoshi4476/seven-HPunyou/settings/secrets/actions で3件登録します。

| 名前 | 値 |
|---|---|
| `GA4_PROPERTY_ID` | STEP2で控えた数字のみ(例: `123456789`) |
| `GA4_CREDENTIALS_JSON` | STEP1でダウンロードしたJSONファイルの中身を**丸ごと**貼り付け |
| `GSC_SITE_URL` | `https://lp.7senses.co.jp/`(**末尾のスラッシュ必須**) |

### STEP 5 — 動作確認

Actions →「月次コンサルレポート」→ Run workflow で手動実行し、
`reports/` に生成されたレポートにアクセス実数が入っていれば成功です。

### サイト無料診断を動かすには

Google Apps Script のコードを [automation/gas/form-endpoint.gs](automation/gas/form-endpoint.gs)
の内容に差し替えて、既存のデプロイを「更新」してください(URLは変わりません)。
このファイルには診断・配信停止・自動返信・ステップメールがすべて入っています。
